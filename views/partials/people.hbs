 <div class = "people">
    <h2>List of people</h2>
    {{#if listExists}}
<ul>
            {{#each people}}
                <li>{{this.name}}: {{this.email}} in {{this.address.zipcode}} </li>
            {{/each}}
        </ul>
    {{/if}}
</div>